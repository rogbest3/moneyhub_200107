<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moneyhub.web.crudtable.CRUDTableMapper">
 	<select id="createCustomer" parameterType="map">
		${CREATE_CUSTOMER}
	</select>
	
	<select id="truncateCustomer" parameterType="map">
		${TRUNCATE_CUSTOMER}
	</select>
	
	<select id="dropCustomer" parameterType="map">
		${DROP_CUSTOMER}
	</select>
	
	<select id="createFee" parameterType="map">   <!-- FEE로 수정 -->
		${CREATE_FEE}
	</select>
	
	<select id="truncateFee" parameterType="map">  <!-- FEE로 수정 -->
		${TRUNCATE_FEE}
	</select>
	
	<select id="dropFee" parameterType="map"> <!-- FEE로 수정 -->
		${DROP_FEE}
	</select>
	
	<select id="createAdmin" parameterType="map">
		${CREATE_ADMIN}
	</select>
		
	<select id="dropAdmin" parameterType="map">
		${DROP_ADMIN}
	</select>
	
	<select id="insertAdmin" parameterType="map">
		${INSERT_ADMIN}
	</select>
	
	<select id="createFeeDB" parameterType="map"> <!-- FEEDB로 수정 -->
		${CREATE_FEEDB}
	</select>
		
	<select id="dropFeeDB" parameterType="map"><!-- FEEDB로 수정 -->
		${DROP_FEEDB}
	</select>
	
	<select id="insertFeeDB" parameterType="map"><!-- FEEDB로 수정 -->
		${INSERT_FEEDB}
	</select>
	
</mapper>